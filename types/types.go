package types

// The type of data we are encoding
type TData = uint64

// An internal type used for various indices
type TIndex = uint64

// An internal type used for various counts
type TCount = uint64

// A type which expresses a symbol
type TSymbol = uint64
